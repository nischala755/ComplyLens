import {NextResponse} from "next/server";import {db} from "@/lib/db";import {getBlastRadius} from "@/lib/services/blastRadius";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const [contact,blastRadius]=await Promise.all([db.contact.findUnique({where:{id},include:{consentRecords:true,processingPurposes:true,assessments:{orderBy:{assessedAt:"desc"},include:{results:true,recommendations:{orderBy:{priority:"asc"}}}}}}),getBlastRadius(id)]);return contact?NextResponse.json({...contact,blastRadius}):NextResponse.json({error:"Not found"},{status:404})}
