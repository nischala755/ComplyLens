import { RULE_CATALOG, SCORING_RATIONALE } from "@/lib/ruleCatalog";

export default function RulesPage(){
  return <><div className="mb-7"><p className="label text-teal-700">Traceability register</p><h1 className="text-3xl font-bold text-[#10233f] mt-2">DPDP rule mapping</h1><p className="text-slate-500 mt-2 max-w-3xl">Each control is mapped to the Act or clearly labelled as an internal operational proxy. This register supports review; it is not legal advice.</p></div>
  <div className="card p-5 mb-6"><h2 className="font-bold text-lg">Scoring model</h2><p className="text-slate-600 mt-2">{SCORING_RATIONALE.model}</p><p className="text-sm text-slate-500 mt-3">{SCORING_RATIONALE.deductions}</p><p className="text-sm text-slate-500 mt-2">{SCORING_RATIONALE.bands}</p></div>
  <div className="space-y-5">{Object.values(RULE_CATALOG).map(rule=><article className="card p-6" key={rule.code}><div className="flex flex-wrap items-center gap-2 mb-4"><span className="font-bold text-[#10233f]">{rule.code} · {rule.name}</span><span className="badge neutral">{rule.severity} · −{rule.deduction}</span><span className={`badge ${rule.mappingType==="direct"?"good":"warn"}`}>{rule.mappingType==="direct"?"Direct statutory mapping":"Operational proxy"}</span></div><dl className="grid gap-4 text-sm"><div><dt className="label">DPDP reference</dt><dd className="mt-1 text-slate-700">{rule.legalReference}</dd></div><div><dt className="label">What the mapped provision requires</dt><dd className="mt-1 text-slate-700">{rule.legalRequirement}</dd></div><div><dt className="label">How ComplyLens tests it</dt><dd className="mt-1 text-slate-700">{rule.testMethod}</dd></div><div><dt className="label">Weight rationale</dt><dd className="mt-1 text-slate-700">{rule.scoringRationale}</dd></div></dl></article>)}</div></>;
}
