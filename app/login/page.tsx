"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{ShieldCheck,LockKeyhole}from"lucide-react";
export default function Login(){const[email,setEmail]=useState("admin@complylens.local"),[password,setPassword]=useState("ComplyLens123!"),[error,setError]=useState(""),[busy,setBusy]=useState(false),r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});setBusy(false);if(res.ok)r.push("/dashboard");else setError((await res.json()).error)}return <main className="min-h-screen grid lg:grid-cols-2 bg-white"><section className="hidden lg:flex bg-[#102747] text-white p-16 flex-col justify-between relative overflow-hidden"><div className="absolute w-96 h-96 bg-teal-400/10 rounded-full -right-32 top-20"/><div className="flex gap-3 items-center relative"><span className="w-11 h-11 bg-[#1ba399] rounded-xl grid place-items-center"><ShieldCheck/></span><div className="text-xl font-bold">ComplyLens</div></div><div className="relative max-w-xl"><p className="text-teal-300 uppercase tracking-[.2em] text-xs font-bold mb-5">Evidence before explanation</p><h1 className="text-5xl font-semibold leading-tight">DPDP compliance you can trace, test, and trust.</h1><p className="mt-6 text-slate-300 text-lg leading-relaxed">Deterministic assessment. Clear remediation. AI explanations grounded only in verified results.</p></div><p className="text-xs text-slate-400">India Digital Personal Data Protection Act, 2023</p></section><section className="grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-sm"><div className="lg:hidden flex items-center gap-2 mb-12 font-bold text-xl"><ShieldCheck className="text-teal-600"/>ComplyLens</div><div className="w-12 h-12 rounded-xl bg-slate-100 grid place-items-center mb-6"><LockKeyhole className="text-[#102747]"/></div><h2 className="text-3xl font-bold text-[#10233f]">Welcome back</h2><p className="text-slate-500 mt-2 mb-8">Sign in to your compliance workspace.</p><label className="label">Email</label><input className="input mt-2 mb-5" value={email} onChange={e=>setEmail(e.target.value)} type="email"/><label className="label">Password</label><input className="input mt-2 mb-4" value={password} onChange={e=>setPassword(e.target.value)} type="password"/>{error&&<p className="text-red-600 text-sm mb-4">{error}</p>}<button disabled={busy} className="btn btn-primary w-full">{busy?"Signing in…":"Sign in"}</button><p className="text-xs text-slate-400 mt-5">Demo credentials are prefilled. Change them before deployment.</p></form></section></main>}
